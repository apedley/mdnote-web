import { Injectable } from '@angular/core';
import { Effect, Actions } from '@ngrx/effects';

@Injectable()
export class AuthEffects {
  // @Effect({ dispatch: false })
  // login = this.actions$.of
}
